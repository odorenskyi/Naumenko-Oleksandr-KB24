#include <iostream>
#include <iomanip>  // Для форматування виводу
#include <clocale>
#include "ModulesNaumenko.h"  // Підключення заголовкового файлу

using namespace std;

// Функція для виводу інформації про розробника
void printDeveloperInfo() {
    cout << "=========================================" << endl;
    cout << " Прізвище Ім'я, email@example.com " << endl;
    cout << " (c) All Rights Reserved " << endl;
    cout << "=========================================" << endl;
}

// Функція для перевірки логічного виразу (a + 1 >= b)
bool checkLogicalExpression(char a, char b) {
    return (a + 1) >= b;
}

// Функція для виводу значень x, y, z у десятковій і шістнадцятковій системі
void printValues(double x, double y, double z) {
    cout << "Десяткова система: x = " << x << ", y = " << y << ", z = " << z << endl;
    cout << "Шістнадцяткова система: "
         << "x = " << hexfloat << x << ", "
         << "y = " << hexfloat << y << ", "
         << "z = " << hexfloat << z << endl;
    cout << defaultfloat; // Повертаємо звичайний формат
}

// Основна функція
int main() {
    setlocale(LC_ALL, "ukr");

    // Виведення інформації про розробника
    printDeveloperInfo();

    // Введення значень x, y, z та символів a, b
    double x, y, z;
    char a, b;
    cout << "Введіть значення x, y, z: ";
    cin >> x >> y >> z;
    cout << "Введіть символи a, b: ";
    cin >> a >> b;

    // Виведення логічного виразу
    bool result = checkLogicalExpression(a, b);
    cout << "Результат виразу (" << a << " + 1 >= " << b << "): "
         << (result ? "true" : "false") << endl;

    // Виведення значень у різних системах числення
    printValues(x, y, z);

    // Обчислення значення S
    double S = s_calculation(x, y, z);
    cout << "Значення S = " << S << endl;

    return 0;
}
